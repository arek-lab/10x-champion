---
const { user } = Astro.locals;
---

<div
  class="mb-4 flex items-center justify-between rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm text-white/80"
>
  {
    user ? (
      <>
        <span class="text-sidebar-foreground/70">{user.email}</span>
        <div class="flex items-center gap-3">
          <a href="/dashboard" class="text-accent hover:text-accent-foreground transition-colors hover:underline">
            Dashboard
          </a>
          <form method="POST" action="/api/auth/signout">
            <button type="submit" class="text-accent hover:text-accent-foreground transition-colors hover:underline">
              Sign out
            </button>
          </form>
        </div>
      </>
    ) : (
      <>
        <span class="text-sidebar-foreground/70">Not signed in</span>
        <div class="flex gap-3">
          <a href="/auth/signin" class="text-accent hover:text-accent-foreground transition-colors hover:underline">
            Sign in
          </a>
          <a href="/auth/signup" class="text-accent hover:text-accent-foreground transition-colors hover:underline">
            Sign up
          </a>
        </div>
      </>
    )
  }
</div>
